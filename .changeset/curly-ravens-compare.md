---
"@aliou/vite-plugin-obsidian": minor
---

Match the CRX-style manifest API by requiring `obsidian({ manifest })` and allowing `defineManifest` to return manifest objects, promises, or Vite environment callbacks.
